name: Telegram Worker

on:
  workflow_dispatch:
    inputs:
      max_titles:
        description: "Max titles per worker"
        default: "1"
      quality:
        description: "Video quality"
        default: "720"
        type: choice
        options: ["1080", "720", "480"]
      workers:
        description: "Number of workers"
        default: "1"
        type: choice
        options: ["1", "2", "3"]

jobs:
  worker:
    runs-on: ubuntu-latest
    timeout-minutes: 330
    strategy:
      matrix:
        n: [1]
      fail-fast: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install -q requests

      - name: Setup Local Telegram Bot API Server
        run: |
          wget -q https://github.com/coyotle/telegram-bot-api-bin/releases/latest/download/telegram-bot-api
          chmod +x telegram-bot-api
          mkdir -p /tmp/tg-data
          ./telegram-bot-api --api-id=${{ secrets.TG_API_ID }} --api-hash=${{ secrets.TG_API_HASH }} --local --dir=/tmp/tg-data &
          sleep 8
          curl -s http://localhost:8081/bot${{ secrets.TG_BOT_TOKEN }}/getMe | head -c 100
          echo ""
          echo "Local Bot API ready (2GB uploads enabled)"

      - name: Run worker
        env:
          GITHUB_TOKEN: ${{ secrets.DB_GITHUB_TOKEN }}
          DB_REPO: ${{ secrets.DB_REPO }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_1: ${{ secrets.TG_CHAT_1 }}
          TG_CHAT_2: ${{ secrets.TG_CHAT_2 }}
          TG_API_BASE: "http://localhost:8081"
          WORKER_ID: "tg-w${{ matrix.n }}-${{ github.run_number }}"
          MAX_TITLES: ${{ github.event.inputs.max_titles || '1' }}
          QUALITY: ${{ github.event.inputs.quality || '720' }}
        run: python tg_worker.py
